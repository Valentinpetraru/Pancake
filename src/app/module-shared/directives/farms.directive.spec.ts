import { FarmsDirective } from './farms.directive';

describe('FarmsDirective', () => {
  it('should create an instance', () => {
    const directive = new FarmsDirective();
    expect(directive).toBeTruthy();
  });
});
